name: bps_sso_sdk
description: A Flutter SDK for BPS (Badan Pusat Statistik) SSO authentication integration
version: 1.6.0
homepage: https://github.com/IPDS-59/bps_sso_flutter_sdk
repository: https://github.com/IPDS-59/bps_sso_flutter_sdk

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

platforms:
  android:
  ios:

dependencies:
  crypto: ^3.0.7
  dio: ^5.9.2
  flutter:
    sdk: flutter
  flutter_custom_tabs: ^2.5.0
  url_launcher: ^6.3.2
  uuid: ^4.5.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0

flutter:
  uses-material-design: true
